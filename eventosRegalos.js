/*function rose_correr(data){

    if (data.giftId === 5655 && data.repeatEnd) {  // Solo procesar cuando termine la racha
        let numberOfRoses = data.repeatCount;  // Cantidad total de rosas enviadas en la racha
        console.log(`${data.uniqueId} ha enviado ${numberOfRoses} rosa(s).`);

    }
}
    

module.exports = { rose_correr };*/
